def LetterCapitalize(str)

  arr = str.split(" ")
  answer = ""
  count = 0
  
  arr.each do |temp|
    temp = temp.capitalize
    answer = answer + temp
    if(count != arr.size - 1)
      answer = answer + " "
    end
    count = count  + 1
  end
  
  return answer
         
end
