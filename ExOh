def ExOh(str)

  xCount = 0
  oCount = 0
  
  str.each_char do |c|
    if(c == 'x')
      xCount = xCount + 1
    elsif(c == 'o')
      oCount = oCount + 1
    end
    
  end
  return xCount == oCount
         
end
